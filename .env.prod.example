# ── Database ──
DB_USER=bronisport
DB_PASSWORD=CHANGE_ME_strong_password_here
DB_NAME=bronisport

# ── JWT (сгенерируй: openssl rand -hex 32) ──
JWT_ACCESS_SECRET=CHANGE_ME_access
JWT_REFRESH_SECRET=CHANGE_ME_refresh

# ── URLs ──
CORS_ORIGIN=https://bronisport.ru
VITE_API_URL=https://bronisport.ru/api

# ── Ports (на сервере) ──
API_PORT=4000
WEB_PORT=3000

# ── Email (Resend) ──
RESEND_API_KEY=re_xxxx
RESEND_FROM=BroniSport <onboarding@resend.dev>

# ── S3 (Yandex Object Storage) ──
S3_ENDPOINT=https://storage.yandexcloud.net
S3_REGION=ru-central1
S3_BUCKET=bronisport-photos
S3_ACCESS_KEY=YCAJExxxxxxxxx
S3_SECRET_KEY=YCPxxxxxxxxxxxxxxxx

# ── Sentry (optional) ──
SENTRY_DSN=
