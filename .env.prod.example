# ── Database ──
DB_USER=bronisport
DB_PASSWORD=CHANGE_ME_strong_password_here
DB_NAME=bronisport

# ── JWT (сгенерируй: openssl rand -hex 32) ──
JWT_ACCESS_SECRET=CHANGE_ME_access
JWT_REFRESH_SECRET=CHANGE_ME_refresh

# ── URLs (подставь свой домен / IP Timeweb) ──
CORS_ORIGIN=https://bronisport.example.com
VITE_API_URL=https://bronisport.example.com/api

# ── Ports (на сервере) ──
API_PORT=4000
WEB_PORT=3000

# ── Email (Resend) ──
RESEND_API_KEY=re_xxxx
RESEND_FROM=BroniSport <onboarding@resend.dev>
